<div class="card">
    <div class="card-header d-flex">
      <div class="card-title flex-grow-1 d-flex justify-content-between align-items-center">
          <h3>Quản lý khách hàng</h3>
          <a href="/sub/<%=hotel._id%>/user/customer/add" class="btn btn-dark">Thêm</a>
      </div>
    </div>
    <!-- /.card-header -->
    <div class="card-body">
      <table id="datatable" class="table table-bordered table-striped">
        <thead>
          <tr>
            <th>STT</th>
            <th>Tên khách hàng</th>
            <th>Số điện thoại</th>
            <th>Email</th>
            <th>Quản lý</th>
          </tr>
        </thead>
        <tbody>
          <% var i=1; %>
          <% for(let user of users){ %>
          <tr>
            <td><%= i++ %></td>
            <td><%= user.name %></td>
            <td><%= user.phone %></td>
            <td><%= user.email %></td>
            <td>
              <a href="/sub/<%=hotel._id%>/user/customer/edit/<%=user._id %>"  class="btn btn-primary">Sửa</a >
              <button  onclick="deleteConfirmButtonClk('/sub/<%=hotel._id%>/user/customer/delete/<%=user._id %>','Bạn có muốn xóa thông tin nhân viên này không?')" class="btn btn-danger">Xóa</button>
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
    <!-- /.card-body -->
  </div>